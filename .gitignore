# Run artifacts — traces are large and regenerable; keep summaries, not raw dumps.
results/**/raw/
results/smoke*.jsonl
*.jsonl.gz

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/

# Secrets — never commit API keys
.env
.env.*
!.env.example

# macOS
.DS_Store
